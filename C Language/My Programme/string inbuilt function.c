#include<stdio.h>
#include<string.h>
int main()
{
    char a[30];
    int i=0,l;
    printf("enter the string\n");
    gets(a);
    l=strlen(a);//count the length of function
    printf("length of a string is %d",l);
}
